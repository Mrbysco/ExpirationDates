* Update to 1.21.1 NeoForge

### The config jsons have been replaced with datapack entries. 
* The format of the JSON stays the same but they are now to be located in `data/expirationdates/<namespace>/expiration_dates/` instead of `config/expirationdates/`.